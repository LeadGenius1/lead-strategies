# Frontend Environment Variables
# Copy this to .env.local and fill in your values
# Local dev: NEXT_PUBLIC_API_URL=http://localhost:3001
# Deployment: Railway only (no Vercel)

# API Endpoints
NEXT_PUBLIC_API_URL=https://api.aileadstrategies.com
NEXT_PUBLIC_WS_URL=wss://api.aileadstrategies.com
NEXT_PUBLIC_KNOWLEDGE_API_URL=https://als-knowledge-agent-production.up.railway.app
NEXT_PUBLIC_VIDEO_API_URL=https://video-assembler-production-6d3b.up.railway.app

# Auth
NEXTAUTH_URL=https://aileadstrategies.com
NEXTAUTH_SECRET=your-nextauth-secret-here
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Stripe (optional - add in Railway Variables when using payments)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_xxx
STRIPE_SECRET_KEY=sk_live_xxx

# Backend URL for API routes (server-side) and OAuth callbacks
BACKEND_URL=https://api.aileadstrategies.com

# ---- Backend-only (.env in backend/ for local, Railway variables for prod) ----
# DATABASE_URL=postgresql://...
# JWT_SECRET=your-jwt-secret
# REDIS_URL=redis://... (optional - in-memory rate limit fallback)
# Stripe Connect (VideoSite): STRIPE_SECRET_KEY, webhook signing
# LeadSite.AI Email: MAILGUN_API_KEY, MAILGUN_DOMAIN
# UltraLead AI: OPENAI_API_KEY
# ClientContact Channels: META_APP_ID, META_APP_SECRET, TWITTER_CLIENT_ID, TWITTER_CLIENT_SECRET
